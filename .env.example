# ==============================================
# GitHub README Stats - Environment Configuration
# ==============================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!
#
# Quick Setup:
# 1. Copy: cp .env.example .env
# 2. Edit .env with your values
# 3. Configure matching GitHub Secrets for CI/CD
#
# ==============================================

# ==============================================
# GitHub Configuration
# ==============================================

# Your GitHub username
GITHUB_USER=your-github-username

# GitHub Container Registry URL (change 'your-username' to your GitHub username)
REGISTRY_URL=ghcr.io/your-github-username

# ==============================================
# Deployment Domain Configuration
# ==============================================

# The domain where your stats will be accessible
# Example: stats.yourdomain.com
DEPLOY_DOMAIN=stats.your-domain.com
STATS_DOMAIN=stats.your-domain.com

# ==============================================
# Port Binding Configuration
# ==============================================

# Bind address (127.0.0.1 for localhost only, 0.0.0.0 for all interfaces)
# Recommended: 127.0.0.1 (let Traefik/reverse proxy handle public access)
BIND_ADDRESS=127.0.0.1

# Port to bind to on the host machine
# Choose an available port (default: 3000, but use unique port to avoid conflicts)
# Check what's available: netstat -tulpn | grep LISTEN
BIND_PORT=3000

# ==============================================
# GitHub Personal Access Tokens (PATs)
# ==============================================

# Required: At least one GitHub Personal Access Token
# Create tokens at: https://github.com/settings/tokens
# Required scopes: 'public_repo', 'read:user'
#
# Rate Limits:
# - No token: 60 requests/hour
# - 1 token (PAT_1): 5,000 requests/hour
# - 4 tokens (PAT_1-4): 20,000 requests/hour (load balanced)

GH_PAT_1=your_github_token_here

# Optional: Additional tokens for higher rate limits
# PAT_2=your_second_github_token
# PAT_3=your_third_github_token
# PAT_4=your_fourth_github_token

# ==============================================
# VPS/Server Configuration (for reference)
# ==============================================

# These values are configured as GitHub Secrets for CI/CD deployment
# You don't need to set them here unless deploying manually
#
# Required GitHub Secrets (set in repo Settings → Secrets and variables → Actions):
# - VPS_HOST: Your VPS IP address or hostname
# - VPS_USER: SSH username for VPS access
# - VPS_KEY: SSH private key for authentication
# - VPS_PASSPHRASE: SSH key passphrase (if your key is encrypted)
# - GHCR_TOKEN: GitHub Container Registry token (for pushing/pulling images)
# - GH_PAT_1: Same as above (for API access in production)

# Local VPS configuration (optional, for manual deployment)
# VPS_HOST=192.168.1.100
# VPS_USER=your-ssh-username
# VPS_PORT=22
# VPS_PASSPHRASE=your-ssh-key-passphrase

# ==============================================
# GitHub Container Registry Token
# ==============================================

# Token for pushing/pulling Docker images from GitHub Container Registry
# Create at: https://github.com/settings/tokens
# Required scopes: 'write:packages', 'read:packages', 'delete:packages'
# GHCR_TOKEN=your_ghcr_token_here

# ==============================================
# Optional: Resource Limits
# ==============================================

# Memory limit for container (e.g., 512M, 1G)
# MEM_LIMIT=512M

# CPU limit (e.g., 0.5 for half a CPU core, 1.0 for one core)
# CPU_LIMIT=0.5

# ==============================================
# Optional: Monitoring Configuration
# ==============================================

# If you have Prometheus monitoring
# PROM_PORT=3000
# PROM_PATH=/api/metrics

# ==============================================
# Optional: Traefik Labels (if using Traefik)
# ==============================================

# Traefik is a reverse proxy that handles SSL/TLS and routing
# TRAEFIK_ENABLE=true
# TRAEFIK_PRIORITY=100
# TRAEFIK_ENTRYPOINT=websecure
# TRAEFIK_CERT_RESOLVER=myresolver

# ==============================================
# Quick Start Checklist
# ==============================================
#
# ✓ 1. Copy this file to .env: cp .env.example .env
# ✓ 2. Set GITHUB_USER to your GitHub username
# ✓ 3. Set REGISTRY_URL (replace 'your-username')
# ✓ 4. Set DEPLOY_DOMAIN and STATS_DOMAIN (your custom domain)
# ✓ 5. Set BIND_PORT (check for conflicts with: netstat -tulpn)
# ✓ 6. Create GitHub Personal Access Token and set GH_PAT_1
# ✓ 7. Configure GitHub Secrets (VPS_HOST, VPS_USER, VPS_KEY, etc.)
# ✓ 8. Set up DNS A record pointing your domain to your VPS IP
# ✓ 9. Ensure Traefik or another reverse proxy is running
# ✓ 10. Test locally: docker compose -f docker-compose.prod.yml up
#
# Need help? Check DOCKER_DEPLOYMENT.md for detailed instructions
#
