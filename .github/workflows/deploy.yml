# ================================================================
# GitHub README Stats - CI/CD Deployment Workflow
# ================================================================
#
# This workflow automatically builds and deploys the application to your VPS
# when you push to master or main branch.
#
# Required GitHub Secrets (Settings → Secrets and variables → Actions):
# - DEPLOY_DOMAIN: Your deployment domain (e.g., stats.yourdomain.com)
# - BIND_PORT: Port to bind on VPS (e.g., 3002)
# - VPS_HOST: Your VPS IP address or hostname
# - VPS_USER: SSH username for VPS
# - VPS_KEY: SSH private key for authentication
# - VPS_PASSPHRASE: SSH key passphrase (if encrypted)
# - GHCR_TOKEN: GitHub Container Registry token
# - GH_PAT_1: GitHub Personal Access Token (required)
# - GH_PAT_2, GH_PAT_3, GH_PAT_4: Additional tokens (optional)
#
# ================================================================

name: Deploy GitHub Readme Stats to VPS

on:
  push:
    branches:
      - master
      - main

permissions:
  contents: read
  packages: write

jobs:
  commit-hash:
    runs-on: ubuntu-latest
    outputs:
      commit_hash: ${{ steps.get_commit.outputs.commit_hash }}
    steps:
      - uses: actions/checkout@v4
      - name: Get commit hash
        id: get_commit
        run: echo "commit_hash=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT

  build-and-push-image:
    needs: commit-hash
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to the Container registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and push Docker image
        run: |
          docker buildx build --platform linux/amd64 --load \
            --build-arg COMMIT_SHA=${{ needs.commit-hash.outputs.commit_hash }} \
            -t ghcr.io/${{ github.repository_owner }}/github-readme-stats:prod -f Dockerfile.prod .
          docker push ghcr.io/${{ github.repository_owner }}/github-readme-stats:prod

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push-image
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy deployment files to VPS
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_KEY }}
          passphrase: ${{ secrets.VPS_PASSPHRASE }}
          port: 22
          source: "docker-compose.prod.yml,DOCKER_SETUP.md,claude.md"
          target: "~/github-readme-stats"

      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_KEY }}
          passphrase: ${{ secrets.VPS_PASSPHRASE }}
          port: 22
          script: |
            cd ~/github-readme-stats

            # Create env file for deployment configuration
            echo "GITHUB_USER=${{ github.repository_owner }}" > .env
            echo "GITHUB_TOKEN=${{ secrets.GHCR_TOKEN }}" >> .env
            echo "REGISTRY_URL=ghcr.io/${{ github.repository_owner }}" >> .env

            # Deployment domain configuration
            echo "DEPLOY_DOMAIN=${{ secrets.DEPLOY_DOMAIN }}" >> .env
            echo "STATS_DOMAIN=${{ secrets.DEPLOY_DOMAIN }}" >> .env
            echo "BIND_ADDRESS=127.0.0.1" >> .env
            echo "BIND_PORT=${{ secrets.BIND_PORT || '3000' }}" >> .env

            # Add GitHub Personal Access Tokens for API access
            echo "PAT_1=${{ secrets.GH_PAT_1 }}" >> .env
            echo "PAT_2=${{ secrets.GH_PAT_2 }}" >> .env
            echo "PAT_3=${{ secrets.GH_PAT_3 }}" >> .env
            echo "PAT_4=${{ secrets.GH_PAT_4 }}" >> .env

            # Verify .env file exists without exposing contents
            if [ -f .env ]; then
              echo "✅ Environment configuration file created successfully"
              echo "📋 Environment variables count: $(grep -c '^[^#]' .env || echo '0')"
            else
              echo "❌ Environment configuration file not found"
            fi

            # Copy the production compose file as main file
            cp docker-compose.prod.yml docker-compose.yml

            # Check if shared monitoring is running
            if ! docker ps | grep -q "traefik"; then
              echo "⚠️  Warning: Traefik not found. Please ensure shared monitoring is running."
              echo "Run: cd ~/shared/monitoring && docker compose up -d"
            fi

            # Login to GitHub Container Registry
            echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.repository_owner }} --password-stdin

            # Stop current containers before starting new ones
            echo "Stopping production containers..."
            docker compose -f docker-compose.yml down || true

            # Start with new configuration
            echo "Starting production container..."
            docker compose pull
            docker compose up -d --force-recreate

            # Check everything is running
            echo "Verifying service is running..."
            docker ps --filter "name=github-readme-stats"

            # Wait a few seconds for startup
            sleep 5

            # Test the production endpoint
            echo "Testing production endpoint:"
            curl -k -s -o /dev/null -w "%{http_code} " https://${{ secrets.DEPLOY_DOMAIN }}/api/ || echo "Not yet available (might be starting)"

            echo "✅ Deployment complete!"
